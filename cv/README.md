# cv

LaTeX CV source, now generated from the YAML data under `../data/en/` and
`../data/gr/` (see `generate/`) rather than hand-edited. Compiled and
published as part of this repo's main build workflow.

Formerly a standalone repo at `github.com/cdalamagkas/cv` (now archived).
